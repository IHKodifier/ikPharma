# ============================================================================
# LIST ALL BUSINESSES
# ============================================================================

query ListAllBusinesses @auth(level: USER) {
  businesses {
    id
    name
    tier
    subscriptionStartDate
    subscriptionEndDate
    isActive
    maxUsers
    maxLocations
    createdAt
    updatedAt
  }
}
