# ============================================================================
# CREATE THERAPEUTIC CLASS
# ============================================================================

mutation CreateTherapeuticClass(
  $code: String!
  $name: String!
  $description: String
) @auth(level: USER) {
  therapeuticClass_insert(data: {
    code: $code
    name: $name
    description: $description
  })
}

