# ============================================================================
# CREATE MANUFACTURER
# ============================================================================

mutation CreateManufacturer(
  $name: String!
  $country: String!
) @auth(level: USER) {
  manufacturer_insert(data: {
    name: $name
    country: $country
  })
}

