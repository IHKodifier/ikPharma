# ============================================================================
# LIST ALL SUPPLIERS
# ============================================================================

query ListAllSuppliers @auth(level: USER) {
  suppliers {
    id
    businessId
    name
    type
    contactPerson
    email
    phone
    paymentTerms
    tier
    outstandingBalance
    isActive
    isPreferred
    createdAt
  }
}
