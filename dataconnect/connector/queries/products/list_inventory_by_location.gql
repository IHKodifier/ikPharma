# ============================================================================
# LIST INVENTORY BY LOCATION
# ============================================================================

query ListInventoryByLocation($locationId: UUID!) @auth(level: USER) {
  inventoryLevels(where: { locationId: { eq: $locationId } }) {
    id
    productId
    locationId
    batchId
    businessId
    quantityOnHand
    quantityReserved
    quantityAvailable
    quantityInTransit
    averageCost
    totalValue
    lastCountDate
    lastDispenseDate
    lastRestockDate
    updatedAt
  }
}
