# ============================================================================
# LIST ALL USERS
# ============================================================================

query ListAllUsers @auth(level: USER) {
  users {
    id
    businessId
    email
    firstName
    lastName
    phone
    role
    isActive
    lastLoginAt
    createdAt
    updatedAt
  }
}
