# ============================================================================
# GET BUSINESS BY ID
# ============================================================================

query GetBusinessById($id: UUID!) @auth(level: USER) {
  business(id: $id) {
    id
    name
    tier
    subscriptionStartDate
    subscriptionEndDate
    isActive
    maxUsers
    maxLocations
    createdAt
    updatedAt
  }
}
