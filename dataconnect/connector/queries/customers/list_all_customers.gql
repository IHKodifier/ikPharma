# ============================================================================
# LIST ALL CUSTOMERS
# ============================================================================

query ListAllCustomers @auth(level: USER) {
  customers {
    id
    businessId
    firstName
    lastName
    email
    phone
    dateOfBirth
    tier
    isActive
    createdAt
  }
}
