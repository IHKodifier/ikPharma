# ============================================================================
# LIST LOCATIONS BY BUSINESS
# ============================================================================

query ListLocationsByBusiness($businessId: UUID!) @auth(level: USER) {
  locations(where: { businessId: { eq: $businessId } }) {
    id
    businessId
    name
    code
    type
    phone
    email
    isActive
    createdAt
  }
}
