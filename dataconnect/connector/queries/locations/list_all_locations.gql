# ============================================================================
# LIST ALL LOCATIONS
# ============================================================================

query ListAllLocations @auth(level: USER) {
  locations {
    id
    businessId
    name
    code
    type
    phone
    email
    isActive
    createdAt
  }
}
